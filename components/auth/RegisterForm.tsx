"use client";
import Link from "next/link";import{FormEvent,useState}from "react";import{Input}from "@/components/ui/Input";import{Button}from "@/components/ui/Button";import{ErrorMessage}from "@/components/ui/ErrorMessage";
export function RegisterForm(){const[loading,setLoading]=useState(false),[error,setError]=useState(""),[done,setDone]=useState(false);const submit=(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);if(String(f.get("password")).length<6){setError("Password must contain at least 6 characters.");return}if(f.get("password")!==f.get("confirm")){setError("Passwords do not match.");return}if(!f.get("terms")){setError("Please accept the terms.");return}setError("");setLoading(true);setTimeout(()=>{setLoading(false);setDone(true)},650)};return <form onSubmit={submit} className="panel auth-card stack"><div><span className="eyebrow">Join Nova</span><h1>Create your account</h1><p className="muted">Save favorites and make checkout easier.</p></div><Input label="Full name" name="name" required/><Input label="Email" name="email" type="email" required/><Input label="Password" name="password" type="password" required/><Input label="Confirm password" name="confirm" type="password" required/><label className="row"><input type="checkbox" name="terms"/> I accept the terms and privacy policy</label>{error&&<ErrorMessage message={error}/>} {done&&<div className="message">Demo account created successfully.</div>}<Button type="submit" size="lg" loading={loading}>Register</Button><p style={{textAlign:"center"}}>Already have an account? <Link href="/login" style={{color:"var(--brand)",fontWeight:800}}>Login</Link></p></form>}
