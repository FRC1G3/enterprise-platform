import Image from "next/image";import{lowStockProducts}from "@/lib/mock-data";export function LowStockList(){return <div>{lowStockProducts.map(p=><div className="spread" style={{padding:"10px 0",borderBottom:"1px solid var(--line)"}} key={p.id}><div className="row"><Image src={p.image} alt={p.name} width={38} height={45} style={{objectFit:"cover",borderRadius:5}}/><div><strong style={{fontSize:13}}>{p.name}</strong><div className="muted" style={{fontSize:11}}>{p.sku}</div></div></div><span className="badge warning">{p.stock} left</span></div>)}</div>}
