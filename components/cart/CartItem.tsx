"use client";
import Image from "next/image";import type{StoreProduct}from "@/lib/mock-data";
export type CartLine={product:StoreProduct;quantity:number;size:string;color:string};
export function CartItem({item,onQuantity,onRemove}:{item:CartLine;onQuantity:(n:number)=>void;onRemove:()=>void}){return <article className="cart-item"><Image src={item.product.image} alt={item.product.name} width={120} height={145}/><div><h3 style={{margin:"2px 0 7px"}}>{item.product.name}</h3><div className="muted">{item.color} · Size {item.size}</div><div className="qty" style={{marginTop:16}}><button type="button" aria-label="Decrease quantity" onClick={()=>onQuantity(Math.max(1,item.quantity-1))}>−</button><span>{item.quantity}</span><button type="button" aria-label="Increase quantity" onClick={()=>onQuantity(item.quantity+1)}>+</button></div><button type="button" className="btn btn-ghost btn-sm" onClick={onRemove} style={{paddingLeft:0}}>Remove</button></div><strong>${(item.product.price*item.quantity).toFixed(2)}</strong></article>}
