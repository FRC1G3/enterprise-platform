import Link from "next/link";import{Button}from "@/components/ui/Button";
export function CartSummary({subtotal}:{subtotal:number}){const shipping=subtotal>=100?0:12,discount=subtotal*.1,total=subtotal+shipping-discount;return <aside className="panel summary"><h2>Order summary</h2><div className="summary-line"><span>Subtotal</span><span>${subtotal.toFixed(2)}</span></div><div className="summary-line"><span>Shipping</span><span>{shipping?"$12.00":"Free"}</span></div><div className="summary-line"><span>Discount</span><span>−${discount.toFixed(2)}</span></div><div className="field" style={{margin:"16px 0"}}><label htmlFor="promo">Promo code</label><div className="row"><input id="promo" className="input" placeholder="NOVA10"/><Button variant="secondary">Apply</Button></div></div><div className="summary-line summary-total"><span>Total</span><span>${total.toFixed(2)}</span></div><Link href="/checkout" className="btn btn-primary btn-lg" style={{width:"100%",marginTop:18}}>Proceed to checkout</Link></aside>}
