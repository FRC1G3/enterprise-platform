"use client";import Link from "next/link";import{Button}from "@/components/ui/Button";export default function ErrorPage({error,reset}:{error:Error&{digest?:string};reset:()=>void}){return <div className="auth-wrap"><div className="panel auth-card" style={{textAlign:"center"}}><span className="eyebrow">Something went wrong</span><h1>We hit an unexpected snag.</h1><p className="muted">{error.message||"Please try again in a moment."}</p><div className="row" style={{justifyContent:"center"}}><Button onClick={reset}>Try again</Button><Link className="btn btn-secondary" href="/">Go home</Link></div></div></div>}
