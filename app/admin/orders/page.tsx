"use client";import Link from "next/link";import{useState}from "react";import{orders}from "@/lib/mock-data";import{OrderStatusBadge}from "@/components/orders/OrderStatusBadge";
export default function AdminOrdersPage(){const[q,setQ]=useState(""),[status,setStatus]=useState(""),[payment,setPayment]=useState("");const rows=orders.filter(o=>(o.id.toLowerCase().includes(q.toLowerCase())||o.customer.toLowerCase().includes(q.toLowerCase()))&&(!status||o.status===status)&&(!payment||o.payment===payment));return <><div className="page-head"><div><span className="eyebrow">Operations</span><h1>Orders</h1><p className="muted">Review and manage customer orders.</p></div></div><div className="admin-filters"><input className="input" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search order or customer" aria-label="Search orders"/><select className="select" value={payment} onChange={e=>setPayment(e.target.value)} aria-label="Payment status"><option value="">All payments</option>{["PAID","PENDING","REFUNDED"].map(x=><option key={x}>{x}</option>)}</select><select className="select" value={status} onChange={e=>setStatus(e.target.value)} aria-label="Order status"><option value="">All statuses</option>{["PENDING","PROCESSING","SHIPPED","DELIVERED","CANCELLED"].map(x=><option key={x}>{x}</option>)}</select><input className="input" type="date" aria-label="Order date"/></div><div className="table-wrap"><table className="table"><thead><tr><th>Order</th><th>Customer</th><th>Date</th><th>Items</th><th>Total</th><th>Payment</th><th>Status</th><th></th></tr></thead><tbody>{rows.map(o=><tr key={o.id}><td><strong>{o.id}</strong></td><td>{o.customer}</td><td>{o.date}</td><td>{o.items}</td><td>${o.total}</td><td><OrderStatusBadge status={o.payment}/></td><td><OrderStatusBadge status={o.status}/></td><td><Link className="btn btn-secondary btn-sm" href={`/admin/orders/${o.id}`}>View</Link></td></tr>)}</tbody></table></div></>}
