import Link from "next/link";import{notFound}from "next/navigation";import{products}from "@/lib/mock-data";import{ProductForm}from "@/components/products/ProductForm";export default async function EditProductPage({params}:{params:Promise<{id:string}>}){const{id}=await params,p=products.find(x=>x.id===id||x.slug===id);if(!p)notFound();return <><div className="page-head"><div><Link href="/admin/products" className="muted">← Products</Link><h1>Edit product</h1><p className="muted">{p.name}</p></div></div><ProductForm product={p}/></>}
