"use client";import{useState}from "react";import{activityLogs}from "@/lib/mock-data";
export default function ActivityLogsPage(){const[q,setQ]=useState(""),[action,setAction]=useState(""),[user,setUser]=useState("");const rows=activityLogs.filter(a=>(a.description.toLowerCase().includes(q.toLowerCase())||a.entity.toLowerCase().includes(q.toLowerCase()))&&(!action||a.action===action)&&(!user||a.user===user));return <><div className="page-head"><div><span className="eyebrow">Audit trail</span><h1>Activity logs</h1><p className="muted">Frontend demonstration of platform activity.</p></div></div><div className="admin-filters"><input className="input" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search activity" aria-label="Search activity"/><select className="select" value={action} onChange={e=>setAction(e.target.value)} aria-label="Filter action"><option value="">All actions</option>{["Updated","Created","Refunded","Alert"].map(x=><option key={x}>{x}</option>)}</select><select className="select" value={user} onChange={e=>setUser(e.target.value)} aria-label="Filter user"><option value="">All users</option><option>Ava Admin</option><option>System</option></select><input className="input" type="date" aria-label="Activity date"/></div><div className="table-wrap"><table className="table"><thead><tr><th>User</th><th>Action</th><th>Entity</th><th>Description</th><th>Date</th><th>IP</th><th>Status</th></tr></thead><tbody>{rows.map(a=><tr key={a.id}><td><strong>{a.user}</strong></td><td>{a.action}</td><td><span className="badge">{a.entity}</span></td><td>{a.description}</td><td>{a.date}</td><td className="muted">{a.ip}</td><td><span className={`badge ${a.status==="Success"?"success":"warning"}`}>{a.status}</span></td></tr>)}</tbody></table></div></>}
