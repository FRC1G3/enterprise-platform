"use client";import{useState}from "react";import{users}from "@/lib/mock-data";
export default function AdminUsersPage(){const[q,setQ]=useState(""),[role,setRole]=useState("");const rows=users.filter(u=>(u.name.toLowerCase().includes(q.toLowerCase())||u.email.toLowerCase().includes(q.toLowerCase()))&&(!role||u.role===role));return <><div className="page-head"><div><span className="eyebrow">Customers</span><h1>Users</h1><p className="muted">View customer accounts and roles.</p></div></div><div className="admin-filters"><input className="input" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search users" aria-label="Search users"/><select className="select" value={role} onChange={e=>setRole(e.target.value)} aria-label="Filter role"><option value="">All roles</option><option>USER</option><option>ADMIN</option></select></div><div className="table-wrap"><table className="table"><thead><tr><th>User</th><th>Role</th><th>Orders</th><th>Total spent</th><th>Status</th><th>Joined</th><th>Actions</th></tr></thead><tbody>{rows.map(u=><tr key={u.id}><td><div className="table-product"><div className="avatar" style={{width:42,height:42,margin:0,fontSize:12}}>{u.name.split(" ").map(x=>x[0]).join("")}</div><div><strong>{u.name}</strong><div className="muted">{u.email}</div></div></div></td><td><span className="badge">{u.role}</span></td><td>{u.orders}</td><td>${u.spent}</td><td><span className={`badge ${u.status==="Active"?"success":""}`}>{u.status}</span></td><td>{u.joined}</td><td><button className="btn btn-secondary btn-sm" type="button">Manage</button></td></tr>)}</tbody></table></div></>}
