"use client";import{useState}from "react";import Link from "next/link";import{orders}from "@/lib/mock-data";import{OrderStatusBadge}from "@/components/orders/OrderStatusBadge";
const statuses=["All","PENDING","PROCESSING","SHIPPED","DELIVERED","CANCELLED"];
export default function OrdersPage(){const[filter,setFilter]=useState("All"),visible=orders.filter(o=>filter==="All"||o.status===filter);return <div className="page"><div className="container"><div className="page-head"><div><span className="eyebrow">My account</span><h1>Order history</h1></div></div><div className="admin-filters">{statuses.map(s=><button className={`btn ${filter===s?"btn-primary":"btn-secondary"} btn-sm`} type="button" key={s} onClick={()=>setFilter(s)}>{s[0]+s.slice(1).toLowerCase()}</button>)}</div><div className="table-wrap"><table className="table"><thead><tr><th>Order</th><th>Date</th><th>Items</th><th>Total</th><th>Payment</th><th>Status</th><th></th></tr></thead><tbody>{visible.map(o=><tr key={o.id}><td><strong>{o.id}</strong></td><td>{o.date}</td><td>{o.items}</td><td>${o.total}</td><td><OrderStatusBadge status={o.payment}/></td><td><OrderStatusBadge status={o.status}/></td><td><Link className="btn btn-secondary btn-sm" href={`/orders/${o.id}`}>View details</Link></td></tr>)}</tbody></table></div></div></div>}
