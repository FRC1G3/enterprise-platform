import Image from "next/image";import{products}from "@/lib/mock-data";import{CheckoutForm}from "@/components/orders/CheckoutForm";
export default function CheckoutPage(){const items=[products[0],products[2]],subtotal=items.reduce((s,p)=>s+p.price,0);return <div className="page section-soft"><div className="container"><div className="page-head"><div><span className="eyebrow">Secure checkout</span><h1>Complete your order</h1></div></div><div className="checkout-layout"><CheckoutForm/><aside className="panel summary"><h2>Order summary</h2>{items.map(p=><div className="spread" style={{padding:"12px 0",borderBottom:"1px solid var(--line)"}} key={p.id}><div className="row"><Image src={p.image} alt={p.name} width={54} height={68} style={{objectFit:"cover",borderRadius:6}}/><div><strong>{p.name}</strong><div className="muted">Qty 1</div></div></div><span>${p.price}</span></div>)}<div className="summary-line"><span>Subtotal</span><span>${subtotal}</span></div><div className="summary-line"><span>Shipping</span><span>Free</span></div><div className="summary-line summary-total"><span>Total</span><span>${subtotal}</span></div><p className="muted" style={{fontSize:12}}>This is a UI demonstration. No real payment or backend request is made.</p></aside></div></div></div>}
